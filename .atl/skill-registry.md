# Skill Registry

## Project Context

- Project root: `/home/lycan/Downloads/ProyectoFinal_TLF`
- Current state: repository initialized with `README.md` and `LICENSE`
- Active planning mode: Spec-Driven Development with filesystem persistence in `openspec/`

## Relevant Skills

| Skill | Purpose | Why it applies |
|------|---------|----------------|
| `sdd-init` | Bootstrap SDD structure | Initializes `openspec/` and project context |
| `sdd-propose` | Define intent and scope | Captures the project start proposal |
| `sdd-spec` | Define requirements and scenarios | Specifies expected behavior before coding |
| `sdd-design` | Describe implementation approach | Documents architecture and branch strategy |
| `sdd-tasks` | Break work into actionable tasks | Organizes execution by phase and owner |

## Project Conventions Detected

- No source code or framework files exist yet.
- No project-level agent instruction files were found.
- Git is initialized on branch `main`.

## Recommended Working Rules

- Use `main` as protected integration branch.
- Use feature branches per contributor: `feature/cristhian/<tema>` and `feature/daniel/<tema>`.
- Keep algorithms explicit, deterministic, and commented as teaching material.
- Prefer custom automata and token validation logic over opaque library helpers.
